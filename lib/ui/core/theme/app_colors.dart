import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Main brand colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color primary = Color(0xFF756EF3);
  static const Color secondary = Color(0xFFD1E2FE);
  static const Color blue = Color(0xFF63B4FF);
  static const Color green = Color(0xFFB1D199);
  static const Color orange = Color(0xFFFFB35A);

  // Text colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFFBDBDBD);

  // Surface colors
  static const Color surface = Color(0xFFF8F9FA);
  static const Color surfaceVariant = Color(0xFFF3F4F6);
  static const Color outline = Color(0xFFE0E0E0);

  // Status colors
  static const Color success = green;
  static const Color warning = orange;
  static const Color error = Color(0xFFFF5757);
  static const Color info = blue;

  // Dark theme colors
  static const Color backgroundDark = Color(0xFF121212);
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color textPrimaryDark = Color(0xFFFFFFFF);
  static const Color textSecondaryDark = Color(0xFFBDBDBD);
  static const Color outlineDark = Color(0xFF2C2C2C);

  // Progress indicator colors
  static const Color progressLow = Color(0xFFE3F2FD);
  static const Color progressMedium = Color(0xFFBBDEFB);
  static const Color progressHigh = Color(0xFF2196F3);

  // Card shadow
  static const Color shadow = Color(0x1A000000);
  static const Color shadowDark = Color(0x40000000);
}